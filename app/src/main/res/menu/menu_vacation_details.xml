<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:id="@+id/save"
        android:title="Save"
        android:icon="@android:drawable/ic_menu_save"
        android:contentDescription="Save"
        />

    <item android:id="@+id/delete"
        android:title="Delete"
        android:icon="@android:drawable/ic_menu_search"
        android:contentDescription="Delete"
        />

    <item android:id="@+id/share"
        android:title="Share"
        android:icon="@android:drawable/ic_menu_search"
        android:contentDescription="Share"
        />

    <item android:id="@+id/start_alert"
        android:title="Start Alert"
        android:icon="@android:drawable/ic_menu_search"
        android:contentDescription="Start Alert Button"
        />

    <item android:id="@+id/end_alert"
        android:title="End Alert"
        android:icon="@android:drawable/ic_menu_search"
        android:contentDescription="End Alert Button"
        />

    <item android:id="@+id/refresh"
        android:title="Refresh"
        android:icon="@android:drawable/ic_menu_search"
        android:contentDescription="Refresh Button"
        />
</menu>